/**
 * Executable entry point for the truthfully limited reference adapter.
 */

import { main } from "@erl2/adapter-sdk";
import { REFERENCE_LIMITED_ADAPTER } from "./adapter.js";

await main(REFERENCE_LIMITED_ADAPTER);
