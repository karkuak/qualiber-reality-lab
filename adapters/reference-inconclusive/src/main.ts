/**
 * Executable entry point for the honestly inconclusive reference adapter.
 */

import { main } from "@erl2/adapter-sdk";
import { REFERENCE_INCONCLUSIVE_ADAPTER } from "./adapter.js";

await main(REFERENCE_INCONCLUSIVE_ADAPTER);
