/**
 * Executable entry point for the misleading reference adapter.
 */

import { main } from "@erl2/adapter-sdk";
import { REFERENCE_MISLEADING_ADAPTER } from "./adapter.js";

await main(REFERENCE_MISLEADING_ADAPTER);
