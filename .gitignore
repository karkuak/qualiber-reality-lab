node_modules/
dist/
*.tsbuildinfo
.erl2-work/
.DS_Store

# Negative-control results are regenerated by `npm run negative-control`; the
# campaign is the evidence, not its last output file.
docs/ledger/negative-controls.json

# Word lock files, which are session state and not documents.
~$*

# ERL2-OQ-005: the pinned OTel Demo archive is fetched, hashed and re-verified
# against the substrate lock; it is an input, not a document.
environments/otel-demo/upstream/
